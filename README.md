# ChromaSense
Cloud-based CNN disease detection raises security and reliability issues, while edge devices lack resources for heavy models. Chroma-Sense processes RGB channels independently using a shared feature extractor, reducing model width, memory use, computations, and storage—enabling efficient plant disease detection on resource-constrained edge devices.
